<h1><%= project.name %></h1>

<a class="btn btn-primary" href="<%= project.githubLink %>" target="_blank">
  <i class="fa fa-github" aria-hidden="true"></i>
  Github Link
</a>

<a class="btn btn-primary" href="<%= project.deployedLink %>" target="_blank">
  <i class="fa fa-globe" aria-hidden="true"></i>
  Deployed Link
</a>

<p><%= project.description %></p>

<p>
   Categories:
   <% for(let i=0; i<project.categories.length; i++) {%>
      <span class="btn btn-default"><%= project.categories[i].name%></span>
   <%}%>
   <form method="post" action="/projects/<%=project.id%>/category">
      <div class="form-group">
         <label for="cat">Add new category</label>
         <input type="text" class="form-control" data-role="tagsinput" id="cat" name="cat">
         <button type="submit" class="btn btn-primary btn-sm">Add Category</button>
      </div>
   </form>
</p>

<hr />

<a href="/">&larr; Back Home</a>
