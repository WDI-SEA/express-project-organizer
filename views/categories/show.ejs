<h1>
    <%= category.name %>
</h1>


<% categories.projects.forEach(project => {
    <a href='/projects/<%= project.id %>'>
        <button>
            <%= project.name %>
        </button>
    </a>
<% }) %>