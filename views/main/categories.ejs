<h1>Categories</h1>

<h2>My Projects</h2>
<hr>

<% if (projects.length < 1) { %>
  <h4>Better get coding, I've no projects!</h4>
<% } %>

<% categories.forEach(function(project) { %>
  <div class="well">
    <h2>
      <a href="/projects/<%= category.name %>"><%= category.name %></a>
    </h2>
  </div>
<% }); %>
